<div class="product-reviews">
    <% @comments.each do |comment| %>
        <div class="row">
            <hr>
            <p class="single-comment"><%= comment.user.first_name %> <small><em><%= "#{time_ago_in_words(comment.created_at)} ago" %></em></small></p>
            <p class="single-comment"><%= "Rating: #{comment.rating}/5" %></p>
            <p class="single-comment"><%= comment.body %></p>
        </div>
    <% end %>
</div>